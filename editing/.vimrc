" vim config file(I like these settings)

set autoindent " 자동 들여쓰기
set smartindent " 스마트 들여쓰기
set cindent " c 들여쓰기
set tabstop=4 " tab을 눌렀을때 인식하는 칸 수
set sw=4
set sts=4
set expandtab " tab 을 space 로 확장
set shiftwidth=4 " >>, << 키로 들여/내어 쓰기할 때, 스페이스 개수
set number " 행번호 표시
set ruler " 커서 위치 표시
set title
set ignorecase " 검색시 대소문자 무시
syntax on " 구문 강조
set hlsearch " 검색어 강조
highlight Comment ctermfg=2 ctermbg=0

